# 2025-11-26 TIL

# ✅ 오늘 한 일
- Frontend Development와 Client-side frameworks 개념 학습
- SPA(Single Page Application) 개념 및 작동 원리 이해
- CSR(Client Side Rendering) 개념 및 작동 원리 학습
- Vue 프레임워크 소개 및 핵심 기능 학습
- Vue의 컴포넌트 기반 아키텍처 이해
- Vue Application 생성 방법 학습 (CDN 방식)
- ref() 함수를 사용한 반응형 상태 선언 실습
- Vue 기본 구조 및 템플릿 렌더링 학습
- v-on 디렉티브를 사용한 이벤트 처리 실습

# 📚 배운 점
- Frontend Development는 사용자가 직접 상호작용하는 UI/UX를 개발하는 것
- Client-side frameworks는 동적인 웹 애플리케이션을 쉽게 구축할 수 있게 도와줌
- SPA는 하나의 HTML 파일 위에서 JavaScript가 필요한 부분만 교체하며 동작
- CSR은 브라우저가 빈 HTML과 JavaScript를 받아 화면을 동적으로 완성하는 방식
- CSR의 장점은 빠른 페이지 전환과 좋은 사용자 경험, 단점은 느린 초기 로드와 SEO 문제
- Vue는 선언적 렌더링과 반응성이 핵심 기능
- createApp()으로 Application instance를 생성하고 mount()로 HTML에 연결
- ref() 함수는 반응형 상태를 선언하며, .value 속성으로 접근 (템플릿에서는 자동 언래핑)
- setup() 함수는 컴포넌트가 동작하기 전 초기 설정을 하는 시작점
- Mustache 구문({{ }})으로 데이터를 템플릿에 동적으로 렌더링
- v-on 디렉티브로 DOM 이벤트를 수신하고 반응형 변수를 변경할 수 있음

# 📌 핵심 개념
- **Frontend Development**: 웹사이트의 UI/UX를 만들고 디자인하는 것
- **Client-side frameworks**: 클라이언트 측에서 UI와 상호작용을 개발하기 위한 JavaScript 기반 프레임워크
- **SPA (Single Page Application)**: 단일 페이지에서 동작하며 페이지 전환 없이 필요한 부분만 업데이트하는 웹 애플리케이션
- **CSR (Client Side Rendering)**: 클라이언트에서 JavaScript를 실행하여 페이지를 렌더링하는 방식
- **Vue**: 사용자 인터페이스를 구축하기 위한 JavaScript 프레임워크
- **선언적 렌더링**: JavaScript 상태를 기반으로 HTML을 선언적으로 작성하는 방식
- **반응성(Reactivity)**: JavaScript 상태 변경을 추적하고 자동으로 DOM을 업데이트하는 기능
- **Component**: 재사용 가능한 UI 코드 블록
- **ref()**: 반응형 상태를 선언하는 함수 (.value 속성으로 접근)
- **setup()**: 컴포넌트 초기 설정을 위한 시작점 함수
- **Mustache 구문({{ }})**: 데이터를 템플릿에 동적으로 렌더링하는 구문
- **v-on 디렉티브**: DOM 이벤트를 수신하는 Vue 디렉티브

# 💡 예시 코드

```html
<!-- 1. Vue Application 생성 기본 구조 -->
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Vue Basic</title>
</head>
<body>
  <!-- Vue 앱이 마운트될 요소 -->
  <div id="app">
    <!-- Mustache 구문으로 데이터 렌더링 -->
    <h1>{{ message }}</h1>
    <p>Count is: {{ count }}</p>
    
    <!-- v-on 디렉티브로 이벤트 처리 -->
    <button v-on:click="increment">증가</button>
    
    <!-- JavaScript 표현식 사용 가능 -->
    <p>{{ count * 2 }}</p>
    <p>{{ message.split('').reverse().join('') }}</p>
  </div>

  <!-- Vue CDN -->
  <script src="https://unpkg.com/vue@3/dist/vue.global.js"></script>
  
  <script>
    // 구조분해할당으로 Vue 객체에서 필요한 함수 가져오기
    const { createApp, ref } = Vue;

    // Application instance 생성
    const app = createApp({
      // setup() 함수: 컴포넌트 초기 설정
      setup() {
        // ref()로 반응형 상태 선언
        const message = ref('Hello Vue!');
        const count = ref(0);
        
        // 이벤트 핸들러 함수
        const increment = function() {
          // .value로 ref 객체의 값에 접근
          count.value++;
        };
        
        // 템플릿에서 사용할 데이터와 함수 반환
        return {
          message,
          count,
          increment
        };
      }
    });

    // HTML 요소에 Vue 앱 연결 (마운트)
    app.mount('#app');
  </script>
</body>
</html>
```

```html
<!-- 2. 반응형 상태 상세 예시 -->
<div id="app">
  <h1>{{ greeting }}</h1>
  <input v-on:input="updateGreeting" type="text">
  <p>입력한 값: {{ userInput }}</p>
</div>

<script src="https://unpkg.com/vue@3/dist/vue.global.js"></script>
<script>
  const { createApp, ref } = Vue;

  const app = createApp({
    setup() {
      const greeting = ref('안녕하세요');
      const userInput = ref('');
      
      const updateGreeting = function(event) {
        // event.target.value로 입력값 가져오기
        userInput.value = event.target.value;
        greeting.value = `안녕하세요, ${event.target.value}님!`;
      };
      
      return {
        greeting,
        userInput,
        updateGreeting
      };
    }
  });

  app.mount('#app');
</script>
```

```javascript
// 3. Vanilla JS vs Vue 비교

// Vanilla JS 방식 (비효율적)
const username1 = document.querySelector('#username1');
const username2 = document.querySelector('#username2');
const username3 = document.querySelector('#username3');
const username4 = document.querySelector('#username4');

inputArea.addEventListener('input', function(e) {
  username1.textContent = e.target.value;
  username2.textContent = e.target.value;
  username3.textContent = e.target.value;
  username4.textContent = e.target.value;
});

// Vue 방식 (효율적)
const { createApp, ref } = Vue;

const app = createApp({
  setup() {
    const username = ref('Unknown User');
    
    return {
      username
    };
  }
});

app.mount('#app');
```

```html
<!-- Vue 템플릿에서는 자동으로 모든 곳에 반영됨 -->
<div id="app">
  <input v-on:input="username = $event.target.value" type="text">
  <h1>안녕하세요 {{ username }}</h1>
  <div>{{ username }}님의 친구 목록</div>
  <div>{{ username }}님의 알림 목록</div>
  <div>{{ username }}님의 친구 요청 목록</div>
</div>
```

```html
<!-- 4. 컴포넌트 구조 예시 -->
<div id="app">
  <!-- Root Component -->
  <h1>{{ title }}</h1>
  
  <!-- 여러 데이터와 메서드 사용 -->
  <p>카운트: {{ count }}</p>
  <button v-on:click="increment">+1</button>
  <button v-on:click="decrement">-1</button>
  <button v-on:click="reset">초기화</button>
</div>

<script src="https://unpkg.com/vue@3/dist/vue.global.js"></script>
<script>
  const { createApp, ref } = Vue;

  const app = createApp({
    setup() {
      // 반응형 상태 여러 개 선언
      const title = ref('카운터 앱');
      const count = ref(0);
      
      // 여러 메서드 정의
      const increment = () => count.value++;
      const decrement = () => count.value--;
      const reset = () => count.value = 0;
      
      // 모두 반환
      return {
        title,
        count,
        increment,
        decrement,
        reset
      };
    }
  });

  app.mount('#app');
</script>
```

# 🛠️ 이슈 & 해결
| 🐞 문제 상황 | 🔍 원인 | 💡 해결 방법 |
|--------------|--------|--------------|
| ref로 선언한 변수의 값을 변경했는데 화면이 업데이트되지 않음 | JavaScript 코드에서 .value를 붙이지 않고 접근함 | ref 객체의 값을 변경할 때는 반드시 .value를 사용해야 함 (예: count.value++) |
| setup() 함수에서 반환하지 않은 변수를 템플릿에서 사용하려 했더니 에러 발생 | setup() 함수에서 return 객체에 포함하지 않으면 템플릿에서 접근 불가 | setup() 함수의 return 문에 템플릿에서 사용할 모든 변수와 함수를 포함해야 함 |
| CSR 방식의 웹사이트가 검색엔진에 잘 노출되지 않음 | 초기 HTML이 거의 비어있어 검색엔진이 콘텐츠를 제대로 읽지 못함 | Next.js, Nuxt.js 같은 하이브리드 프레임워크를 사용하거나, SSR(Server Side Rendering)을 고려 |
| mount()를 여러 번 호출했더니 에러 발생 | 각 앱 인스턴스는 mount()를 한 번만 호출할 수 있음 | 하나의 앱 인스턴스당 mount()는 딱 한 번만 호출. 여러 곳에 마운트하려면 별도의 앱 인스턴스를 생성 |
| Vanilla JS와 비교했을 때 Vue가 왜 더 효율적인지 이해 안 됨 | 데이터가 변경될 때마다 일일이 DOM을 찾아 수정해야 하는 번거로움 | Vue는 반응형 시스템으로 데이터가 변경되면 자동으로 관련된 모든 DOM을 업데이트하므로 코드가 간결하고 관리하기 쉬움 |