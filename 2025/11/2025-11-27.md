# 2025-11-27 TIL

# ✅ 오늘 한 일
- Vue Template Syntax 학습 (Text Interpolation, Raw HTML, Attribute Bindings, JavaScript Expressions)
- Directive 개념 및 구조 학습
- v-bind를 사용한 동적 데이터 바인딩 실습
- Class와 Style 바인딩 (객체/배열 구문) 학습
- v-on을 사용한 이벤트 핸들링 실습
- Event Modifiers와 Key Modifiers 학습
- v-model을 사용한 양방향 바인딩 실습
- Form Input Bindings (Checkbox, Select 등) 활용 실습

# 📚 배운 점
- Template Syntax는 HTML을 기반으로 Vue가 제공하는 특별한 문법
- Directive는 v- 접두사를 가진 특수 속성으로, DOM에 반응형 동작을 적용하는 명령어
- v-bind는 콜론(:)으로 약어 사용 가능하며, HTML 속성을 Vue 데이터와 동적으로 연결
- Class 바인딩은 객체나 배열 구문을 사용하여 조건부로 클래스를 적용할 수 있음
- Style 바인딩은 camelCase와 kebab-case 모두 지원하지만 camelCase 권장
- v-on은 @로 약어 사용 가능하며, DOM 이벤트를 수신하고 핸들러를 실행
- Inline handlers는 간단한 로직에, Method handlers는 복잡한 로직에 사용
- Event Modifiers(.prevent, .stop 등)로 이벤트 동작을 제어할 수 있음
- v-model은 v-bind와 v-on의 조합으로 양방향 바인딩을 구현
- IME 언어(한국어 등)에서는 v-model이 제대로 업데이트되지 않아 v-bind와 v-on 방식 사용 권장
- $event 변수로 Inline handlers에서도 DOM 이벤트 객체에 접근 가능

# 📌 핵심 개념
- **Template Syntax**: Vue가 DOM을 컴포넌트 인스턴스의 데이터에 선언적으로 바인딩할 수 있는 HTML 기반 구문
- **Directive**: v- 접두사가 있는 특수 속성으로, DOM에 반응형 동작을 적용하는 명령어
- **v-bind**: HTML 속성을 Vue 데이터와 동적으로 바인딩하는 디렉티브 (약어: :)
- **동적 인자**: 대괄호([])로 감싸서 JavaScript 표현식을 디렉티브 인자로 사용
- **Class Bindings**: 객체나 배열을 사용하여 클래스를 동적으로 바인딩
- **Style Bindings**: 객체나 배열을 사용하여 인라인 스타일을 동적으로 바인딩
- **v-on**: DOM 이벤트를 수신하고 핸들러를 연결하는 디렉티브 (약어: @)
- **Inline handlers**: 이벤트 발생 시 실행될 JavaScript 코드를 직접 작성
- **Method handlers**: setup()에 정의된 메서드를 호출하는 방식
- **Event Modifiers**: .prevent, .stop 등 이벤트 동작을 제어하는 특별한 접미사
- **Key Modifiers**: 특정 키보드 키에 대한 이벤트를 수신하는 수식어
- **v-model**: 폼 요소에 양방향 바인딩을 만드는 디렉티브 (v-bind + v-on의 조합)
- **$event**: Inline handlers에서 DOM 이벤트 객체에 접근하기 위한 특별 변수

# 💡 예시 코드

```html
<!-- 1. Template Syntax 예시 -->
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Template Syntax</title>
</head>
<body>
  <div id="app">
    <!-- Text Interpolation (콧수염 구문) -->
    <p>Message: {{ msg }}</p>
    
    <!-- Raw HTML -->
    <div v-html="rawHtml"></div>
    
    <!-- Attribute Bindings -->
    <div v-bind:id="dynamicId"></div>
    <div :id="dynamicId"></div> <!-- 약어 -->
    
    <!-- JavaScript Expressions -->
    <p>{{ number + 1 }}</p>
    <p>{{ ok ? 'YES' : 'NO' }}</p>
    <p>{{ message.split('').reverse().join('') }}</p>
    <div :id="`list-${id}`"></div>
  </div>

  <script src="https://unpkg.com/vue@3/dist/vue.global.js"></script>
  <script>
    const { createApp, ref } = Vue;

    const app = createApp({
      setup() {
        const msg = ref('Hello Vue!');
        const rawHtml = ref('<span style="color:red">This should be red.</span>');
        const dynamicId = ref('my-id');
        const number = ref(10);
        const ok = ref(true);
        const message = ref('Vue.js');
        const id = ref(1);
        
        return {
          msg,
          rawHtml,
          dynamicId,
          number,
          ok,
          message,
          id
        };
      }
    });

    app.mount('#app');
  </script>
</body>
</html>
```

```html
<!-- 2. v-bind - Attribute Bindings -->
<div id="app">
  <!-- 기본 속성 바인딩 -->
  <img :src="imageSrc" alt="Random Image">
  <a :href="myUrl">Move to url</a>
  
  <!-- 동적 인자 -->
  <button :[key]="myValue">Dynamic Attribute</button>
  <p :[dynamicAttr]="dynamicValue"></p>
</div>

<script src="https://unpkg.com/vue@3/dist/vue.global.js"></script>
<script>
  const { createApp, ref } = Vue;

  const app = createApp({
    setup() {
      const imageSrc = ref('https://picsum.photos/200');
      const myUrl = ref('https://www.google.co.kr/');
      const key = ref('id');
      const myValue = ref('my-button');
      const dynamicAttr = ref('title');
      const dynamicValue = ref('Hello Vue.js');
      
      return {
        imageSrc,
        myUrl,
        key,
        myValue,
        dynamicAttr,
        dynamicValue
      };
    }
  });

  app.mount('#app');
</script>
```

```html
<!-- 3. Class Bindings -->
<div id="app">
  <!-- 객체 구문 -->
  <div :class="{ active: isActive }">Text 1</div>
  <div class="static" :class="{ active: isActive, 'text-primary': hasInfo }">Text 2</div>
  
  <!-- 객체를 변수로 분리 -->
  <div class="static" :class="classObj">Text 3</div>
  
  <!-- 배열 구문 -->
  <div :class="[activeClass, infoClass]">Text 4</div>
  
  <!-- 배열 + 객체 혼합 -->
  <div :class="[{ active: isActive }, infoClass]">Text 5</div>
</div>

<style>
  .active { background-color: yellow; }
  .text-primary { color: blue; }
  .static { border: 1px solid black; }
</style>

<script src="https://unpkg.com/vue@3/dist/vue.global.js"></script>
<script>
  const { createApp, ref } = Vue;

  const app = createApp({
    setup() {
      const isActive = ref(true);
      const hasInfo = ref(true);
      const activeClass = ref('active');
      const infoClass = ref('text-primary');
      
      const classObj = ref({
        active: isActive,
        'text-primary': hasInfo
      });
      
      return {
        isActive,
        hasInfo,
        activeClass,
        infoClass,
        classObj
      };
    }
  });

  app.mount('#app');
</script>
```

```html
<!-- 4. Style Bindings -->
<div id="app">
  <!-- 객체 구문 (camelCase) -->
  <div :style="{ color: activeColor, fontSize: fontSize + 'px' }">Text 1</div>
  
  <!-- 객체 구문 (kebab-case) -->
  <div :style="{ color: activeColor, 'font-size': fontSize + 'px' }">Text 2</div>
  
  <!-- 객체를 변수로 분리 -->
  <div :style="styleObj">Text 3</div>
  
  <!-- 배열 구문 (여러 스타일 객체 병합) -->
  <div :style="[styleObj, styleObj2]">Text 4</div>
</div>

<script src="https://unpkg.com/vue@3/dist/vue.global.js"></script>
<script>
  const { createApp, ref } = Vue;

  const app = createApp({
    setup() {
      const activeColor = ref('crimson');
      const fontSize = ref(50);
      
      const styleObj = ref({
        color: activeColor,
        fontSize: fontSize.value + 'px'
      });
      
      const styleObj2 = ref({
        color: 'blue',
        border: '1px solid black'
      });
      
      return {
        activeColor,
        fontSize,
        styleObj,
        styleObj2
      };
    }
  });

  app.mount('#app');
</script>
```

```html
<!-- 5. v-on - Event Handling -->
<div id="app">
  <!-- Inline handlers -->
  <button @click="count++">Add 1</button>
  <p>Count: {{ count }}</p>
  
  <!-- Method handlers -->
  <button @click="increase">Increase</button>
  <button @click="myFunc">Hello</button>
  
  <!-- 사용자 지정 인자 전달 -->
  <button @click="greeting('hello')">Say hello</button>
  <button @click="greeting('bye')">Say bye</button>
  
  <!-- $event로 이벤트 객체 접근 -->
  <button @click="warning('경고입니다', $event)">Warning</button>
  <button @click="danger('위험', $event, '합니다')">Danger</button>
  
  <!-- Event Modifiers -->
  <form @submit.prevent="onSubmit">
    <input type="submit" value="Submit">
  </form>
  
  <!-- 버블링 제어 -->
  <div @click="detectBubble">
    <a href="https://www.google.com/">Link 1 (버블링 O)</a><br>
    <a @click.prevent="onLink" href="https://www.google.com/">Link 2 (prevent만)</a><br>
    <a @click.stop.prevent="onLink" href="https://www.google.com/">Link 3 (stop + prevent)</a>
  </div>
  
  <!-- Key Modifiers -->
  <input @keyup.enter="onSubmit" placeholder="Press Enter">
  <textarea @keydown.ctrl.enter="submitComment" placeholder="Ctrl + Enter to submit"></textarea>
</div>

<script src="https://unpkg.com/vue@3/dist/vue.global.js"></script>
<script>
  const { createApp, ref } = Vue;

  const app = createApp({
    setup() {
      const count = ref(0);
      const name = ref('Alice');
      
      const increase = function() {
        count.value += 1;
      };
      
      const myFunc = function(event) {
        console.log(event);
        console.log(event.currentTarget);
        console.log(`Hello ${name.value}!`);
      };
      
      const greeting = function(message) {
        console.log(message);
      };
      
      const warning = function(message, event) {
        console.log(message);
        console.log(event);
      };
      
      const danger = function(msg1, event, msg2) {
        console.log(msg1);
        console.log(event);
        console.log(msg2);
      };
      
      const onSubmit = function() {
        console.log('onSubmit');
      };
      
      const detectBubble = function() {
        console.log('detectBubble');
      };
      
      const onLink = function() {
        console.log('onLink');
      };
      
      const submitComment = function() {
        console.log('Comment submitted');
      };
      
      return {
        count,
        name,
        increase,
        myFunc,
        greeting,
        warning,
        danger,
        onSubmit,
        detectBubble,
        onLink,
        submitComment
      };
    }
  });

  app.mount('#app');
</script>
```

```html
<!-- 6. v-model - Form Input Bindings -->
<div id="app">
  <!-- v-bind + v-on 방식 -->
  <p>{{ inputText1 }}</p>
  <input :value="inputText1" @input="onInput">
  
  <!-- v-model 방식 (간편함) -->
  <p>{{ inputText2 }}</p>
  <input v-model="inputText2">
  
  <!-- Checkbox - 단일 -->
  <input type="checkbox" id="checkbox" v-model="checked">
  <label for="checkbox">{{ checked }}</label>
  
  <!-- Checkbox - 여러 개 (배열) -->
  <div>Checked names: {{ checkedNames }}</div>
  <input type="checkbox" id="alice" value="Alice" v-model="checkedNames">
  <label for="alice">Alice</label>
  <input type="checkbox" id="bella" value="Bella" v-model="checkedNames">
  <label for="bella">Bella</label>
  
  <!-- Select -->
  <div>Selected: {{ selected }}</div>
  <select v-model="selected">
    <option disabled value="">Please select one</option>
    <option>Alice</option>
    <option>Bella</option>
    <option>Cathy</option>
  </select>
</div>

<script src="https://unpkg.com/vue@3/dist/vue.global.js"></script>
<script>
  const { createApp, ref } = Vue;

  const app = createApp({
    setup() {
      const inputText1 = ref('');
      const inputText2 = ref('');
      const checked = ref(false);
      const checkedNames = ref([]);
      const selected = ref('');
      
      const onInput = function(event) {
        inputText1.value = event.currentTarget.value;
      };
      
      return {
        inputText1,
        inputText2,
        checked,
        checkedNames,
        selected,
        onInput
      };
    }
  });

  app.mount('#app');
</script>
```

# 🛠️ 이슈 & 해결
| 🐞 문제 상황 | 🔍 원인 | 💡 해결 방법 |
|--------------|--------|--------------|
| 콧수염 구문을 HTML 속성 안에서 사용했더니 동작하지 않음 | 콧수염 구문은 HTML 속성 내에서 사용할 수 없음 | v-bind 디렉티브를 사용하여 속성을 바인딩 (예: `<div :id="dynamicId">`) |
| 동적 인자에 대문자를 사용했더니 제대로 작동하지 않음 | 브라우저가 속성 이름을 소문자로 강제 변환함 | 동적 인자는 반드시 소문자로만 작성 (예: :[myattr] ✅, :[myAttr] ❌) |
| Event Modifiers를 여러 개 사용했는데 순서가 중요한지 몰랐음 | Modifiers는 작성된 순서대로 실행됨 | @click.stop.prevent와 @click.prevent.stop은 다른 결과를 낼 수 있으므로 순서에 유의 |
| 한국어 입력 시 v-model이 제대로 업데이트되지 않음 | IME(한국어, 중국어, 일본어) 언어는 v-model이 조합 중에 업데이트되지 않음 | v-bind와 v-on을 함께 사용하거나 .lazy 수식어 사용 (단, 실시간 반영 안 됨) |
| Inline handlers에서 event 객체에 접근하려 했으나 방법을 몰랐음 | 일반적인 event 변수로는 접근 불가 | $event 특별 변수를 사용하여 접근 (예: @click="warning('메시지', $event)") |