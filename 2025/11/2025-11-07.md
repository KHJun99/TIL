# 2025-11-07 TIL

# ✅ 오늘 한 일
- Git Branch의 개념과 필요성 학습
- Branch 관련 명령어(git branch, git switch) 실습
- HEAD의 개념과 역할 이해
- Fast-Forward Merge와 3-Way Merge 차이점 학습 및 실습
- Merge Conflict 발생 원인과 해결 방법 실습
- Feature Branch Workflow 협업 방식 학습
- Git Flow 브랜치 관리 전략 학습
- Forking Workflow (Fork & Pull model) 학습

# 📚 배운 점
- **Branch의 핵심 가치**: 독립적인 작업 공간을 제공하여 master 브랜치에 영향을 주지 않고 안전하게 작업 가능
- **git switch 주의사항**: Working Directory의 파일이 모두 버전 관리되고 있는지 확인 필요, git add 하지 않은 새 파일은 Git 관리를 받지 않아 브랜치 이동 시에도 유지됨
- **commit 진행 방향**: commit은 이전 commit에 종속되어 생성되므로 화살표는 이전 commit을 가리킴
- **HEAD의 역할**: 현재 브랜치나 commit을 가리키는 포인터로 현재 위치를 나타냄
- **Fast-Forward Merge**: 분기 없이 일직선상에 있을 때 포인터만 이동시키는 방식
- **3-Way Merge**: 병합하는 각 브랜치의 commit 2개와 공통 조상 commit 1개를 사용하여 병합, merge commit 생성
- **Merge Conflict**: 같은 파일의 같은 부분을 동시에 수정했을 때 발생, 3-Way Merge에서만 발생
- **Pull Request의 진정한 목적**: 코드를 합치기 전 팀의 관리자가 변경된 코드를 검토하고 피드백을 줄 수 있는 공식적인 코드 리뷰 장
- **Git Flow의 핵심**: 5가지 브랜치(master, develop, feature, release, hotfix) 각각의 명확한 역할 분담
- **Shared repository vs Fork & Pull**: 소유권이 있는 경우 Feature Branch Workflow, 소유권이 없는 경우(오픈소스) Forking Workflow 사용

# 📌 핵심 개념
- **Branch**: 나뭇가지처럼 여러 갈래로 작업 공간을 나누어 독립적으로 작업할 수 있는 Git 도구
- **HEAD**: 현재 브랜치나 commit을 가리키는 포인터, 현재 내가 바라보는 위치
- **Fast-Forward Merge**: 브랜치 포인터를 대상 브랜치 상태로 이동시키는 "빨리 감기" 방식
- **3-Way Merge**: 두 브랜치의 commit + 공통 조상 commit 사용, 새로운 merge commit 생성
- **Merge Conflict**: 같은 파일의 같은 부분을 동시에 수정하여 Git이 판단하지 못할 때 발생
- **Pull Request(PR)**: 별도 브랜치의 작업을 중심 브랜치에 병합해달라고 요청하는 기능, 코드 리뷰의 핵심 도구
- **Feature Branch Workflow**: Shared repository model, 원격저장소 소유권 공유
- **Git Flow 5가지 브랜치**:
  - master: 실제 프로덕션 배포, 가장 안정적
  - develop: 다음 릴리즈 개발 기지
  - feature: 특정 기능 개발 작업실
  - release: 배포 전 최종 점검
  - hotfix: 긴급 버그 수정
- **Forking Workflow**: Fork & Pull model, 소유권 없는 원격저장소를 개인 계정으로 복제
- **upstream**: 원본 원격 저장소를 가리키는 remote 이름

# 💡 예시 코드

**Branch 생성 및 조회**
```bash
# 브랜치 목록 조회
git branch

# 원격 브랜치 목록 조회
git branch -r

# 새로운 브랜치 생성
git branch login
git branch feature/signup

# 브랜치 삭제 (병합된 브랜치만)
git branch -d login

# 브랜치 강제 삭제
git branch -D login
```

**Branch 이동**
```bash
# 브랜치 이동
git switch login
git switch master

# ⚠️ switch 전 주의사항
# Working Directory의 모든 파일이 Git 관리 중인지 확인
git status  # Untracked files가 없는지 확인
git add .   # 필요시 모든 파일 staging
```

**Branch 실습 예제**
```bash
# 1. 사전 준비
mkdir git-branch-practice
cd git-branch-practice
git init

# 2. article.txt 생성 및 commit 작성
echo "master-1" > article.txt
git add .
git commit -m "master-1"

echo "master-2" >> article.txt
git add .
git commit -m "master-2"

echo "master-3" >> article.txt
git add .
git commit -m "master-3"

# 3. commit 목록 확인
git log --oneline
# 결과:
# 0604dcd (HEAD -> master) master-3
# 9c22c89 master-2
# 3d71510 master-1

# 4. login 브랜치 생성
git branch login

# 5. 브랜치 목록 확인
git branch
# 결과:
#   login
# * master  (현재 HEAD가 가리키는 브랜치)

# 6. master에서 추가 commit
echo "master-4" >> article.txt
git add .
git commit -m "master-4"

# 7. login 브랜치로 이동
git switch login

# 8. login 브랜치에서 작업
echo "login-1" >> article.txt
git add .
git commit -m "login-1"

# 9. 전체 commit 목록 확인 (그래프)
git log --oneline --all --graph
# 결과:
# * 3b0a091 (HEAD -> login) login-1
# | * 5ca7701 (master) master-4
# |/
# * 0604dcd master-3
# * 9c22c89 master-2
# * 3d71510 master-1
```

**Fast-Forward Merge 실습**
```bash
# 1. 사전 준비
mkdir fast-forward-practice
cd fast-forward-practice
git init

# 2. master에서 commit 작성
echo "master-1" > article.txt
git add .
git commit -m "master-1"

echo "master-2" >> article.txt
git add .
git commit -m "master-2"

# 3. hotfix 브랜치 생성 및 이동
git branch hotfix
git switch hotfix

# 4. hotfix에서 commit 작성
echo "hotfix-1" >> article.txt
git add .
git commit -m "hotfix-1"

echo "hotfix-2" >> article.txt
git add .
git commit -m "hotfix-2"

# 5. master로 이동 후 병합
git switch master
git merge hotfix
# 결과: Fast-forward (포인터만 이동)

# 6. 병합 완료된 브랜치 삭제
git branch -d hotfix
```

**3-Way Merge 실습**
```bash
# 1. 사전 준비
mkdir 3-way-merge-practice
cd 3-way-merge-practice
git init

# 2. master에서 commit 작성
echo "c1" > article.txt
git add .
git commit -m "c1"

echo "c2" >> article.txt
git add .
git commit -m "c2"

# 3. hotfix 브랜치 생성 및 이동
git branch hotfix
git switch hotfix

# 4. hotfix에서 작업
echo "c3" >> article.txt
git add .
git commit -m "c3"

echo "c4" > hotfix-article.txt
git add .
git commit -m "c4"

# 5. master로 돌아와서 추가 작업
git switch master
echo "c5" >> article.txt
git add .
git commit -m "c5"

# 6. hotfix 브랜치 병합
git merge hotfix
# 결과: Merge made by the 'ort' strategy.
# 새로운 merge commit 생성됨

# 7. commit 목록 확인
git log --oneline --all --graph
# 결과:
# * 5640903 (HEAD -> master) Merge branch 'hotfix'
# |\
# | * 66aafib c4
# | * 468fc94 c3
# * | 03b1a9f c5
# |/
# * 03b1a9f c2
# * 0f44a67 c1

# 8. 병합 완료된 브랜치 삭제
git branch -d hotfix
```

**Merge Conflict 해결 실습**
```bash
# 1. 사전 준비
mkdir merge-conflict-practice
cd merge-conflict-practice
git init

# 2. master에서 commit 작성
echo "master-1" > article.txt
git add .
git commit -m "master-1"

echo "master-2" >> article.txt
git add .
git commit -m "master-2"

# 3. hotfix 브랜치 생성 및 이동
git branch hotfix
git switch hotfix

# 4. hotfix에서 같은 파일 수정
echo "hotfix-1" >> article.txt
git add .
git commit -m "hotfix-1"

# 5. master로 돌아와서 같은 부분 수정
git switch master
echo "master-3" >> article.txt
git add .
git commit -m "master-3"

echo "master-4" >> article.txt
git add .
git commit -m "master-4"

# 6. 병합 시도 -> 충돌 발생!
git merge hotfix
# 결과:
# Auto-merging article.txt
# CONFLICT (content): Merge conflict in article.txt
# Automatic merge failed; fix conflicts and then commit the result.

# 7. 상태 확인
git status
# 결과: both modified: article.txt

# 8. article.txt 파일 열어보면:
# master-1
# master-2
# <<<<<<< HEAD
# master-3
# master-4
# =======
# hotfix-1
# >>>>>>> hotfix

# 9. 충돌 해결 (원하는 내용으로 수정)
# 충돌 마커 제거하고 내용 정리
# master-1
# master-2
# master-3
# master-4
# hotfix-1

# 10. 충돌 해결 완료
git add article.txt
git commit  # vim 에디터에서 :wq 입력하여 저장 및 종료

# 11. 브랜치 삭제
git branch -d hotfix
```

**Conflict 마커 이해하기**
```
<<<<<<< HEAD
현재 브랜치(master)에서 작성한 내용
=======
병합하려는 브랜치(hotfix)에서 작성한 내용
>>>>>>> hotfix

해결 방법:
1. 둘 중 하나 선택
2. 둘 다 포함
3. 완전히 새로운 내용으로 수정
4. 충돌 마커(<<<<<<<, =======, >>>>>>>) 모두 제거
```

**Feature Branch Workflow**
```bash
# 1. 원격 저장소 clone
git clone <repository-url>
cd <repository-name>

# 2. 기능별 브랜치 생성
git branch feature/login
git switch feature/login

# 3. 기능 개발
echo "login feature" > login.txt
git add .
git commit -m "feat: Add login feature"

# 4. 원격 저장소에 브랜치 push
git push origin feature/login

# 5. GitHub에서 Pull Request 생성
# - base: master, compare: feature/login
# - 코드 리뷰 진행

# 6. PR 병합 후 최신 master 가져오기
git switch master
git pull origin master

# 7. 로컬에서 병합 완료된 브랜치 삭제
git branch -d feature/login

# 8. 원격 브랜치 삭제 (옵션)
git push origin --delete feature/login

# 9. 새로운 기능 개발 시작
git branch feature/signup
git switch feature/signup
# 작업 반복...
```

**Git Flow 브랜치 전략**
```bash
# 1. 초기 설정
git init
git commit -m "Initial commit"

# 2. develop 브랜치 생성
git branch develop
git switch develop

# 3. feature 브랜치에서 기능 개발
git branch feature/login
git switch feature/login
# 개발 진행...
git add .
git commit -m "feat: Implement login"

# 4. develop에 병합
git switch develop
git merge feature/login
git branch -d feature/login

# 5. release 브랜치 생성 (배포 준비)
git branch release/v1.0
git switch release/v1.0
# 버그 수정, 버전 번호 업데이트...
git commit -m "chore: Prepare v1.0 release"

# 6. master에 병합 (배포)
git switch master
git merge release/v1.0
git tag v1.0

# 7. develop에도 병합 (중요!)
git switch develop
git merge release/v1.0
git branch -d release/v1.0

# 8. 긴급 버그 발생 시 hotfix
git switch master
git branch hotfix/critical-bug
git switch hotfix/critical-bug
# 버그 수정...
git commit -m "fix: Critical bug"

# 9. master에 병합 및 재배포
git switch master
git merge hotfix/critical-bug
git tag v1.0.1

# 10. develop에도 병합 (중요!)
git switch develop
git merge hotfix/critical-bug
git branch -d hotfix/critical-bug
```

**Forking Workflow**
```bash
# 1. GitHub에서 원본 저장소를 Fork (웹에서 진행)

# 2. Fork한 저장소를 로컬에 clone
git clone <내-fork-repository-url>
cd <repository-name>

# 3. 원본 저장소를 upstream으로 연결
git remote add upstream <원본-repository-url>

# 4. remote 확인
git remote -v
# origin    <내-fork-url> (fetch)
# origin    <내-fork-url> (push)
# upstream  <원본-url> (fetch)
# upstream  <원본-url> (push)

# 5. 기능 브랜치 생성 및 작업
git branch feature/new-feature
git switch feature/new-feature
# 작업 진행...
git add .
git commit -m "feat: Add new feature"

# 6. 내 원격 저장소(origin)에 push
git push origin feature/new-feature

# 7. GitHub에서 Pull Request 생성
# - base repository: 원본 저장소
# - base branch: master
# - head repository: 내 fork 저장소
# - compare branch: feature/new-feature

# 8. 코드 리뷰 및 원본 저장소에서 병합 완료

# 9. 로컬 브랜치 삭제
git switch master
git branch -d feature/new-feature

# 10. 원본 저장소의 최신 코드 가져오기
git pull upstream master

# 11. 내 fork 저장소도 업데이트
git push origin master

# 12. 새로운 기능 개발 시작
git branch feature/another-feature
# 과정 반복...
```

**유용한 Git 명령어 조합**
```bash
# 그래프 형태로 모든 브랜치 로그 보기
git log --oneline --all --graph

# 특정 브랜치의 commit 개수 확인
git rev-list --count <branch-name>

# 브랜치 간 차이 확인
git diff master..feature/login

# 병합 취소 (충돌 발생 시)
git merge --abort

# 마지막 commit 수정
git commit --amend

# 원격 브랜치 목록 갱신
git fetch --prune
```

# 🛠️ 이슈 & 해결
| 🐞 문제 상황 | 🔍 원인 | 💡 해결 방법 |
|--------------|--------|--------------|
| git switch 후 새로 만든 파일이 다른 브랜치에도 존재 | git add 하지 않아 Git 버전 관리를 받지 않는 파일은 브랜치 이동 시에도 유지됨 | switch 전에 반드시 `git add .` 또는 `git commit` 실행하여 모든 파일을 Git 관리 대상으로 만들기 |
| Merge conflict 발생 시 어떤 내용을 선택해야 할지 모름 | 충돌 마커의 의미를 정확히 이해하지 못함 | `<<<<<<< HEAD`는 현재 브랜치, `>>>>>>> branch-name`은 병합하려는 브랜치. `=======`를 기준으로 위/아래 구분 |
| Fast-Forward Merge와 3-Way Merge의 차이를 이해하지 못함 | 브랜치가 분기했는지 여부에 따라 merge 방식이 달라짐 | Fast-Forward는 일직선(분기 없음), 3-Way는 분기가 있어서 merge commit 필요 |
| release 브랜치에서 버그 수정 후 develop에 병합하지 않음 | Git Flow에서 release와 hotfix의 변경사항은 develop에도 반영 필수 | release 또는 hotfix 브랜치의 변경사항은 master와 develop 양쪽 모두에 병합 필요 |
| Pull Request 생성 후 바로 master에 병합함 | PR의 진정한 목적이 코드 리뷰라는 것을 인식하지 못함 | PR 생성 후 팀원들의 코드 리뷰를 받고 승인 후 병합 진행 |
| Forking Workflow에서 upstream과 origin 혼동 | remote의 역할 이해 부족 | upstream은 원본 저장소, origin은 내 fork 저장소. upstream에서 pull, origin에 push |
| Git Flow에서 feature 브랜치를 master에 직접 병합 시도 | Git Flow의 브랜치 흐름 이해 부족 | feature → develop → release → master 순서. feature는 절대 master와 직접 상호작용 안 함 |